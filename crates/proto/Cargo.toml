[package]
name = "winstats-proto"
version = "0.2.0"
edition = "2021"
rust-version = "1.77"
description = "Wire types shared by winstats and winstats-agent"
license = "MIT"
publish = false

[dependencies]
serde = { version = "=1.0.210", features = ["derive"] }
serde_json = "=1.0.128"
